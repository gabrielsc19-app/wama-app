"use client";
import { ArrowLeft, ChevronRight, Pencil, Plus, Users } from "lucide-react";
type Ref = { id: string; name: string; color?: unknown; description?: unknown; members?: unknown; receives_urgent?: unknown };
type Person = { id: string; full_name: string; email: string };
type Case = { id: string; case_number: string; title: string; status: string; is_urgent: boolean; priority: string; team?: Ref; assignee?: Person; due_at: string };
export default function OperationsTeamWorkspace({ teams, cases, members, selectedId, select, openCase, edit, invite }: { teams: Ref[]; cases: Case[]; members: Person[]; selectedId: string | null; select: (id: string | null) => void; openCase: (item: Case) => void; edit: (item: Ref) => void; invite: () => void }) {
  const team = teams.find((item) => item.id === selectedId);
  if (!team) return <section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{teams.map((item) => { const teamCases = cases.filter((entry) => entry.team?.id === item.id); const open = teamCases.filter((entry) => !["closed", "cancelled"].includes(entry.status)).length; return <button key={item.id} onClick={() => select(item.id)} className="rounded-[2rem] border border-[#DCE1E6] bg-white p-6 text-left transition hover:-translate-y-1 hover:border-[#00B8AE] hover:shadow-lg"><span className="block h-2 w-16 rounded-full" style={{ background: String(item.color || "#00B8AE") }}/><h3 className="mt-5 text-xl font-black">{item.name}</h3><p className="mt-2 text-sm text-[#69717D]">{String(item.description || (item.receives_urgent ? "Recibe alertas urgentes" : "Casos regulares"))}</p><div className="mt-5 flex items-center justify-between"><strong className="text-sm">{open} abiertos · {teamCases.length} total</strong><span className="inline-flex items-center gap-1 text-xs font-black text-[#008F87]">Entrar y gestionar <ChevronRight className="h-4 w-4"/></span></div></button>; })}</section>;
  const rawMembers = (team.members || []) as Array<{ profile_id: string; team_role: string }>;
  const teamCases = cases.filter((entry) => entry.team?.id === team.id);
  return <div className="grid gap-5"><button onClick={() => select(null)} className="inline-flex w-fit items-center gap-2 rounded-full border bg-white px-4 py-2 text-sm font-black"><ArrowLeft className="h-4 w-4"/>Volver a equipos</button><section className="rounded-[2rem] bg-[#0B0C0E] p-6 text-white sm:p-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div><span className="block h-2 w-16 rounded-full" style={{ background: String(team.color || "#00E5D6") }}/><h2 className="mt-4 text-3xl font-black">{team.name}</h2><p className="mt-2 text-sm text-[#C4C7CC]">{String(team.description || "Gestión operacional del equipo")}</p></div><div className="flex flex-wrap gap-2"><button onClick={invite} className="inline-flex items-center gap-2 rounded-full bg-[#00E5D6] px-4 py-3 text-sm font-black text-black"><Plus className="h-4 w-4"/>Invitar integrante</button><button onClick={() => edit(team)} className="inline-flex items-center gap-2 rounded-full border border-white/30 px-4 py-3 text-sm font-black"><Pencil className="h-4 w-4"/>Editar equipo</button></div></div></section><div className="grid gap-4 sm:grid-cols-4"><Metric label="Casos abiertos" value={teamCases.filter((item) => !["closed", "cancelled"].includes(item.status)).length}/><Metric label="Urgentes" value={teamCases.filter((item) => item.is_urgent && !["closed", "cancelled"].includes(item.status)).length}/><Metric label="Cerrados" value={teamCases.filter((item) => item.status === "closed").length}/><Metric label="Integrantes" value={rawMembers.length}/></div><section className="rounded-[2rem] border bg-white p-6"><div className="flex items-center gap-2"><Users className="text-[#008F87]"/><h3 className="text-xl font-black">Integrantes</h3></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{rawMembers.map((membership) => { const person = members.find((item) => item.id === membership.profile_id); return <div key={membership.profile_id} className="rounded-2xl bg-[#F4F6F7] p-4"><strong className="block">{person?.full_name || "Usuario pendiente"}</strong><span className="text-xs text-[#69717D]">{person?.email || "Invitación pendiente"}</span><span className="mt-2 block text-xs font-black text-[#008F87]">{membership.team_role === "coordinator" ? "Coordinador" : "Integrante"}</span></div>; })}{!rawMembers.length && <p className="text-sm text-[#69717D]">Este equipo aún no tiene integrantes.</p>}</div></section><section className="overflow-hidden rounded-[2rem] border bg-white"><div className="border-b p-6"><h3 className="text-xl font-black">Casos del equipo</h3></div><div className="divide-y">{teamCases.map((item) => <button key={item.id} onClick={() => openCase(item)} className="flex w-full items-center justify-between gap-4 p-5 text-left hover:bg-[#F7FAFA]"><div><span className="text-xs font-black text-[#008F87]">{item.case_number}</span><strong className="mt-1 block">{item.title}</strong><span className="mt-1 block text-xs text-[#69717D]">{item.assignee?.full_name || "Sin responsable"} · {new Date(item.due_at).toLocaleString("es-CL")}</span></div><span className="inline-flex items-center gap-1 text-xs font-black">Ver y gestionar <ChevronRight className="h-4 w-4"/></span></button>)}{!teamCases.length && <p className="p-10 text-center text-sm text-[#69717D]">Este equipo aún no tiene casos.</p>}</div></section></div>;
}
function Metric({ label, value }: { label: string; value: number }) { return <div className="rounded-3xl border bg-white p-5"><span className="text-xs font-black uppercase text-[#69717D]">{label}</span><strong className="mt-2 block text-3xl">{value}</strong></div>; }
